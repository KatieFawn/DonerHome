<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DonerHome"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <activity
            android:name=".activities.intro.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.menu.MenuActivity"
            android:exported="true">
        </activity>

        <activity android:name=".activities.menu.DealsActivity"
            android:exported="true">
        </activity>

        <activity android:name=".activities.menu.shopping.CartActivity"
            android:exported="true">
        </activity>

        <activity android:name=".activities.menu.profile.ProfileActivity"
            android:exported="true">
        </activity>

            <activity android:name=".activities.menu.profile.UserInfoActivity"
                android:exported="true">
            </activity>

            <activity android:name=".activities.menu.profile.AddressesActivity"
                android:exported="true">
            </activity>

                <activity android:name=".activities.menu.profile.UpdateAddressActivity"
                    android:exported="true">
                </activity>

            <activity android:name=".activities.menu.profile.FeedBackActivity"
                android:exported="true">
            </activity>

            <activity android:name=".activities.menu.profile.SettingsActivity"
                android:exported="true">
            </activity>

                <activity android:name=".activities.menu.profile.LanguageActivity"
                    android:exported="true">
                </activity>

        <activity android:name=".activities.auth.LoginActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>